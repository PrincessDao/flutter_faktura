import 'package:flutter/material.dart';

class Screens3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Экран Иннокентия'),
    );
  }
}